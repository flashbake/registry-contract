import { TezosToolkit } from "@taquito/taquito";
import NetworkConfig from "./types/network-config";
import { InMemorySigner } from "@taquito/signer";
export declare const getSigner: (config: NetworkConfig) => Promise<InMemorySigner>;
export declare const getTezos: (config: NetworkConfig) => Promise<TezosToolkit>;
