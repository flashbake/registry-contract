import { BigNumber } from 'bignumber.js';
import { OperationBatch } from "@taquito/taquito/dist/types/batch/rpc-batch-provider";
import NetworkConfig from './types/network-config';
import { TezosToolkit } from '@taquito/taquito';
declare function sendOperation(config: NetworkConfig, Tezos: TezosToolkit, contractAddress: string, entrypoint: string, parameter: string | BigNumber | null | any[], amount?: number, batch?: OperationBatch): Promise<string | undefined>;
export default sendOperation;
