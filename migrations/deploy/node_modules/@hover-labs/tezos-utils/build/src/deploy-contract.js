"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const check_confirmed_1 = __importDefault(require("./check-confirmed"));
const utils_1 = require("./utils");
async function deployContract(config, Tezos, contractSource, storage) {
    try {
        console.log(`Using storage: ${storage}`);
        const result = await Tezos.contract.originate({
            code: contractSource,
            init: storage,
        });
        console.log(`Deployed in hash: ${result.hash}`);
        console.log(`Deployed contract: ${result.contractAddress}`);
        console.log('');
        console.log("Awaiting confirmation...");
        await (0, check_confirmed_1.default)(config, result.hash);
        console.log("Confirmed!");
        return {
            operationHash: result.hash,
            contractAddress: result.contractAddress || "ERR",
        };
    }
    catch (e) {
        console.log('Caught exception, retrying...');
        console.error(e);
        debugger;
        console.log(e.message);
        await (0, utils_1.sleep)(config.operationDelaySecs);
        return deployContract(config, Tezos, contractSource, storage);
    }
}
exports.default = deployContract;
//# sourceMappingURL=deploy-contract.js.map